services:
  - type: web
    name: bitunix-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python simulator.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
